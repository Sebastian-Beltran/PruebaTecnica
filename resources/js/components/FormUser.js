import React from 'react';
import ReactDOM from 'react-dom';


export default class FormUser extends Component {
    render(){
        return (
            <h1>Hola</h1>
        );
    }
}

// if (document.getElementById('form_user')) {
//     ReactDOM.render(<FormUser />, document.getElementById('form_user'));
// }